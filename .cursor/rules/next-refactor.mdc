---
description: Vite React SPA migration has shipped. See ARCHITECTURE.md. Do not start a second static client.
alwaysApply: false
---

# Vite React refactor — done

The static `js/` + `css/` client was replaced. Do not revive it.

Current stack: Vite + React + TypeScript + Tailwind + Lucide, Supabase email Auth, `public.scraps` + `scrap-media`, Firebase Hosting + Cloud Function `/api/analyze` with `claude-sonnet-4-5` (assignment name `claude-sonnet-5` is not a current id). Env: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `ANTHROPIC_API_KEY`.

Keep the product: personal scrap shelf, KO/EN, DESIGN.md kitchen. Not a recipe app. Not a dual client.
